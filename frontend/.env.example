# Frontend environment (Vercel)
# Point this to your deployed backend. Include protocol only if not standard.
# Example: your-backend.onrender.com  (NO http/https, NO trailing slash)
NEXT_PUBLIC_BACKEND_URL=localhost:8000